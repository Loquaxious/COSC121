def vertical_strings(strings):
    """Returns the string vertically the number of times the length of
    the string"""
    for s in strings:
        string2 = s * len(strings)
        print(string2) 