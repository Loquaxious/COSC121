def print_names(names):
    """Print the names in the list of names, one per line"""
    i = 0
    while i < len(names):
        print(names[i])
        i += 1 

print_names(['John', 'Mary', 'Donald'])
print_names(['Victor'])