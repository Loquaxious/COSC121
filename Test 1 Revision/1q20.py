# ???? How ???
def ranked(scores, rank):
    """L"""
    result = []
    max_score = 0 
    number_of_ranks = len(scores)
    for player in scores:
        for score, name in tuple1:
        

data = [(10, 'A'), (30, 'C'), (20, 'B')]
print(ranked(data, 1))